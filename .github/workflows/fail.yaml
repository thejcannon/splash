---
name: Try not to fail

on:
    pull_request:
        types: [opened, edited, reopened]

jobs:
    try_not_to_fail:
        runs-on: ubuntu-latest
        steps:
            - name: just fail lol
              continue-on-error: true
              run: exit 1

